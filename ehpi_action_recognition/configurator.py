from ehpi_action_recognition.configs import config


def setup_application():
    # Example to change config stuff, call this method before everything else.
    # config.cache_config.cache_dir = "abc"
    a = 1
